const auth = require('./authSchemas');
const users = require('./userSchemas');
const books = require('./booksSchemas');
const training = require('./trainingSchemas');

module.exports = {
  auth,
  users,
  books,
  training,
};
